import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.archventures.archformslab',
  appName: 'ArchForms Lab',
  webDir: 'dist'
};

export default config;
